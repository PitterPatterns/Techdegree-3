const name = document.getElementById('name');
const other = document.querySelector('other-title');
const colors = document.getElementById('color');

name.focus();




colors.textContent = "Please select a T-shirt theme";